"use client";

import * as React from "react";
import { Mail, Megaphone, Plus, Send, Trash2, Users } from "lucide-react";
import { useRouter } from "next/navigation";
import { toast } from "sonner";
import { createEmailCampaign, createNewProductCampaign, deleteEmailCampaign, queueEmailCampaign, sendCampaignTest } from "@/lib/actions/index";
import type { getMarketingDashboard } from "@/lib/actions/index";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Textarea } from "@/components/ui/textarea";

type Dashboard = Awaited<ReturnType<typeof getMarketingDashboard>>;

export function MarketingManager({ data }: { data: Dashboard }) {
  const router = useRouter();
  const [open, setOpen] = React.useState(false);
  const [pending, startTransition] = React.useTransition();
  const active = data.subscribers.filter((subscriber) => subscriber.status === "active").length;
  function run(task: () => Promise<unknown>, success: string) { startTransition(async () => { try { await task(); toast.success(success); setOpen(false); router.refresh(); } catch (error) { toast.error(error instanceof Error ? error.message : "Operation failed."); } }); }
  function create(formData: FormData) { run(() => createEmailCampaign(formData), "Campaign saved."); }
  return <div className="space-y-6"><div className="flex flex-wrap items-end justify-between gap-4"><div><h1 className="font-heading text-3xl font-semibold">Email Marketing</h1><p className="mt-1 text-sm text-muted-foreground">Consent-based subscribers, campaigns, and durable delivery operations.</p></div><Button onClick={() => setOpen(true)}><Plus className="mr-2 size-4" />New campaign</Button></div><div className="grid gap-4 sm:grid-cols-3"><Stat icon={<Users />} label="Active subscribers" value={active} /><Stat icon={<Megaphone />} label="Campaigns" value={data.campaigns.length} /><Stat icon={<Mail />} label="Queued / failed" value={(data.queue.pending ?? 0) + (data.queue.failed ?? 0)} /></div><Tabs defaultValue="campaigns"><TabsList><TabsTrigger value="campaigns">Campaigns</TabsTrigger><TabsTrigger value="subscribers">Subscribers</TabsTrigger><TabsTrigger value="queue">Delivery queue</TabsTrigger></TabsList><TabsContent value="campaigns" className="space-y-4"><Card><CardHeader><CardTitle>New-product campaign</CardTitle><CardDescription>Create a professional draft directly from an active product.</CardDescription></CardHeader><CardContent><div className="flex flex-wrap gap-2">{data.products.slice(0, 12).map((product) => <Button key={product.id} variant="outline" size="sm" disabled={pending} onClick={() => run(() => createNewProductCampaign(product.id), "New-product campaign created.")}>{product.title}</Button>)}</div></CardContent></Card>{data.campaigns.map((campaign) => <Card key={campaign.id}><CardHeader className="flex-row items-start justify-between"><div><CardTitle className="text-lg">{campaign.name}</CardTitle><CardDescription>{campaign.subject}</CardDescription></div><Badge variant={campaign.status === "sent" ? "default" : "secondary"}>{campaign.status}</Badge></CardHeader><CardContent><div className="flex flex-wrap items-center gap-2 text-xs text-muted-foreground"><span>{campaign.type.replace("_", " ")}</span><span>{campaign.recipientCount} recipients</span>{campaign.scheduledAt && <span>Scheduled {new Date(campaign.scheduledAt).toLocaleString()}</span>}</div><div className="mt-4 flex flex-wrap gap-2">{["draft", "scheduled"].includes(campaign.status) && <Button size="sm" disabled={pending} onClick={() => run(() => queueEmailCampaign(campaign.id), "Campaign queued.")}><Send className="mr-1 size-4" />Send now</Button>}<Button size="sm" variant="outline" disabled={pending} onClick={() => { const email = window.prompt("Test recipient email"); if (email) run(() => sendCampaignTest(campaign.id, email), "Test email queued."); }}>Send test</Button>{campaign.status === "draft" && <Button size="icon-sm" variant="ghost" className="text-destructive" disabled={pending} onClick={() => run(() => deleteEmailCampaign(campaign.id), "Campaign deleted.")}><Trash2 /></Button>}</div></CardContent></Card>)}</TabsContent><TabsContent value="subscribers"><Card><CardHeader><CardTitle>Subscribers</CardTitle><CardDescription>Double opt-in status and consent source.</CardDescription></CardHeader><CardContent className="space-y-3">{data.subscribers.map((subscriber) => <div key={subscriber.id} className="flex flex-wrap items-center justify-between gap-3 rounded-lg border p-3"><div><p className="font-medium">{subscriber.email}</p><p className="text-xs text-muted-foreground">{subscriber.source} · {subscriber.locale} · {new Date(subscriber.createdAt).toLocaleDateString()}</p></div><Badge variant={subscriber.status === "active" ? "default" : "secondary"}>{subscriber.status}</Badge></div>)}</CardContent></Card></TabsContent><TabsContent value="queue"><Card><CardHeader><CardTitle>Queue health</CardTitle><CardDescription>The authenticated cron claims due jobs every 10 minutes with retries and dead-letter handling.</CardDescription></CardHeader><CardContent><div className="grid gap-3 sm:grid-cols-5">{["pending", "processing", "sent", "failed", "dead"].map((status) => <div key={status} className="rounded-lg border p-3"><p className="text-xs capitalize text-muted-foreground">{status}</p><p className="text-2xl font-semibold">{data.queue[status] ?? 0}</p></div>)}</div></CardContent></Card></TabsContent></Tabs><Dialog open={open} onOpenChange={setOpen}><DialogContent className="max-h-[90vh] overflow-y-auto sm:max-w-2xl"><form onSubmit={(event) => { event.preventDefault(); create(new FormData(event.currentTarget)); }}><DialogHeader><DialogTitle>New email campaign</DialogTitle><DialogDescription>Create a draft or schedule delivery to all active subscribers.</DialogDescription></DialogHeader><div className="grid gap-4 py-5"><Field name="name" label="Campaign name" required /><Field name="subject" label="Email subject" required /><Field name="previewText" label="Preview text" /><div className="grid gap-4 sm:grid-cols-2"><Select name="type" label="Type" options={["newsletter", "new_product"]} /><Field name="scheduledAt" label="Schedule (optional)" type="datetime-local" /></div><div className="space-y-2"><Label htmlFor="content">Email HTML</Label><Textarea id="content" name="content" rows={10} required placeholder="<h1>Store update</h1><p>Your message...</p>" /></div></div><DialogFooter><Button type="button" variant="outline" onClick={() => setOpen(false)}>Cancel</Button><Button disabled={pending}>Save campaign</Button></DialogFooter></form></DialogContent></Dialog></div>;
}

function Stat({ icon, label, value }: { icon: React.ReactNode; label: string; value: number }) { return <Card><CardContent className="flex items-center gap-3 p-5"><span className="text-primary [&>svg]:size-5">{icon}</span><div><p className="text-2xl font-semibold">{value}</p><p className="text-xs text-muted-foreground">{label}</p></div></CardContent></Card>; }
function Field({ name, label, ...props }: React.ComponentProps<typeof Input> & { name: string; label: string }) { return <div className="space-y-2"><Label htmlFor={name}>{label}</Label><Input id={name} name={name} {...props} /></div>; }
function Select({ name, label, options }: { name: string; label: string; options: string[] }) { return <div className="space-y-2"><Label htmlFor={name}>{label}</Label><select id={name} name={name} className="w-full rounded-md border bg-background px-3 py-2 text-sm">{options.map((option) => <option key={option}>{option}</option>)}</select></div>; }
